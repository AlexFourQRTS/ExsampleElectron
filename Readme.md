# Electron Example

Electron + React + MUI.

```bash
npm install
```

## Dev

```bash
npm run dev
```

## Build (compile only)

```bash
npm run build
```

## Make app

Packages a native app into `dist/` (`.app` + `.dmg` on macOS, installer on Windows, AppImage on Linux):

```bash
npm run make
```
